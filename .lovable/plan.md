

User wants:
1. Cap supplier image import at 6 per color
2. Fix the issue where only 2 product images show (currently using `image_front`/`image_back`/`image_side1`/`image_side2` — likely only front/back are being populated by importers)

Let me check the SanMar importer and Products UI to confirm scope.
